---
basic_ssh::server::permitrootlogin: 'yes'

inventory_networks:
  ipv4:
    vty_admin:
      vlan_id: '1000'
      vlan_name: 'vty-admin'
      comment: [ 'blibli blibli' ]
      network: '172.31.0.0'
      netmask: '255.255.0.0'
      broadcast: '172.31.255.255'
      gateway: '172.31.0.1'
  ipv6: {}

network:
  interfaces:
    eth0:
      networks: [ 'vty-admin' ]
      comment: [ 'blabla blabla...' ]
      inet:
        method: 'static'
        option:
          address: '172.31.2.3'
          netmask: '%{::ipv4_networks.vty_admin.netmask}'
          network: '%{::ipv4_networks.vty_admin.network}'
          broadcast: '%{::ipv4_networks.vty_admin.broadcast}'
          gateway: '%{::ipv4_networks.vty_admin.gateway}'
          truc: '__default__'


#network::interfaces::interfaces:
#  ethHIERA:
#    method: 'dhcp'


#azerty: 'azerty'

key1: [ 'aa%{::datacenter}a',
        'bbb',
      ]

